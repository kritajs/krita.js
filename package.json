{
  "name": "kritajs",
  "version": "0.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "workspaces": [
    "deps/ultralight"
  ],
  "devDependencies": {
    "@types/node": "^22.7.4"
  }
}
